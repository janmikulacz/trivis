name: trivis_map_editor
channels:
  - conda-forge
  - defaults
dependencies:
  # C++ toolchain and build tools
  - compilers==1.7.0
  - gcc=12.3
  - cmake==3.28.1
  - git
  - pybind11>=2.11
  # Python runtime (uv manages packages on top of this)
  - python>=3.10
  - uv>=0.4
